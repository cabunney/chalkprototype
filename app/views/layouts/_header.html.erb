<header class="navbar">
  <div class="navbar-inner">
    <div class="container">
      <nav>
      	<ul class = "nav pull-left">
      	<li id = "site_title">Chalk</li>
		<% if signed_in?%>
		<li class = "navelement-left"><%=current_user.school_name%></li>	
		<%end%>
		</ul>
        <ul class="nav pull-right">
          <li class = "navelement-right <%= 'active' if params[:controller] == 'message_board' %>"><%= link_to "chalkboard", 	    {:controller => :message_board, :action => :show}%></li>
          <% if signed_in? %>
            <!-- <li id="fat-menu" class="dropdown">
                          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            Submit <b class="caret"></b>
                          </a>
                          <ul class="dropdown-menu">
                            <li><%= link_to "Question", :controller => :submission, :action => :QS %></li>
                            <li><%= link_to "Idea",  :controller => :submission, :action => :AS %></li>
                          </ul>
                        </li> -->
			<li  class="dropdown navelement-right">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                <%=current_user.first_name + " " + current_user.last_name%> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu navelement-sub">
                <li>
                  <%= link_to "sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>

